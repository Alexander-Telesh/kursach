# База знаний по серии книг "Стеллар"

Веб-приложение на Streamlit для базы знаний по серии книг "Стеллар" с текстовым поиском, интеграцией с AuthorToday API для отзывов, и возможностью чтения и скачивания книг в формате FB2.

## 🚀 Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd Kursach
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте переменные окружения:**
   - Создайте файл `.env` на основе `.env.example`
   - Заполните необходимые переменные (см. раздел "Настройка")

4. **Создайте таблицы в Supabase:**
   - Откройте Supabase Dashboard → SQL Editor
   - Выполните SQL из файла `sql/create_tables.sql`

5. **Добавьте книги:**
   ```bash
   python scripts/add_books_from_files.py
   ```

6. **Запустите приложение:**
   ```bash
   streamlit run app.py
   ```

## Возможности

- 📚 Просмотр информации о книгах серии "Стеллар"
- 🔍 Текстовый поиск по базе знаний
- 💬 Отзывы с ресурса AuthorToday с динамическим обновлением
- 📖 Чтение книг в формате FB2 онлайн
- ⬇️ Скачивание книг в формате FB2
- 📊 Статистика по серии и рейтинги

## Технологии

- **Python 3.9+**
- **Streamlit** - веб-фреймворк
- **Supabase** - база данных PostgreSQL
- **Supabase Python SDK** - работа с базой данных через REST API
- **MCP Supabase** - интеграция для разработки через ИИ-ассистентов (Cursor)
- **AuthorToday API** - интеграция для получения отзывов
- **BeautifulSoup/lxml** - парсинг FB2 файлов

## Установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Supabase настройки
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_anon_key_или_service_role_key
SUPABASE_DB_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# AuthorToday API (опционально, для получения отзывов)
AUTHORTODAY_LOGIN=ваш_логин
AUTHORTODAY_PASSWORD=ваш_пароль
```

### 3. Настройка Supabase

1. Зарегистрируйтесь на [Supabase](https://supabase.com)
2. Создайте новый проект
3. Скопируйте URL проекта и ключ из **Settings → API**
4. Получите connection string из **Settings → Database**
   - Рекомендуется использовать **Connection pooling** (порт 6543)
5. Выполните SQL из `sql/create_tables.sql` в **SQL Editor**
6. Настройте Row Level Security (RLS) политики (см. раздел "Настройка RLS")

### 4. Настройка AuthorToday (опционально)

1. Зарегистрируйтесь на [AuthorToday](https://author.today)
2. Используйте логин и пароль для авторизации в API
3. Добавьте их в `.env` файл

### 5. Добавление книг

Поместите FB2 файлы книг в папку `books/`, затем:

```bash
python scripts/add_books_from_files.py
```

## Настройка Row Level Security (RLS)

Для работы приложения необходимо настроить политики RLS в Supabase. Выполните в **SQL Editor**:

```sql
-- Политики для таблицы books
ALTER TABLE books ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow public read access on books"
ON books FOR SELECT TO public USING (true);

CREATE POLICY "Allow public insert on books"
ON books FOR INSERT TO public WITH CHECK (true);

CREATE POLICY "Allow public update on books"
ON books FOR UPDATE TO public USING (true) WITH CHECK (true);

CREATE POLICY "Allow public delete on books"
ON books FOR DELETE TO public USING (true);

-- Политики для таблицы reviews
ALTER TABLE reviews ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow public read access on reviews"
ON reviews FOR SELECT TO public USING (true);

CREATE POLICY "Allow public insert on reviews"
ON reviews FOR INSERT TO public WITH CHECK (true);

CREATE POLICY "Allow public update on reviews"
ON reviews FOR UPDATE TO public USING (true) WITH CHECK (true);

CREATE POLICY "Allow public delete on reviews"
ON reviews FOR DELETE TO public USING (true);
```

**Альтернатива:** Используйте Service Role Key в `SUPABASE_KEY` (обходит RLS, но менее безопасно).

## Запуск локально

```bash
streamlit run app.py
```

Приложение будет доступно по адресу `http://localhost:8501`

## Структура базы данных

### Таблица `books`
- `id` - первичный ключ
- `title` - название книги
- `author` - автор
- `description` - описание
- `series_order` - порядок в серии
- `fb2_file_path` - путь к FB2 файлу
- `litres_book_id` - ID книги в AuthorToday (историческое поле)
- `created_at`, `updated_at` - временные метки

### Таблица `reviews`
- `id` - первичный ключ
- `book_id` - внешний ключ на books
- `litres_review_id` - ID отзыва в AuthorToday
- `author_name` - имя автора отзыва
- `rating` - оценка (1-5)
- `text` - текст отзыва
- `date` - дата отзыва
- `created_at`, `updated_at` - временные метки

## Развертывание на Streamlit Cloud

1. **Загрузите проект на GitHub:**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push -u origin main
   ```

2. **Зарегистрируйтесь на [Streamlit Cloud](https://streamlit.io/cloud)**

3. **Подключите репозиторий GitHub**

4. **Добавьте переменные окружения в настройках приложения:**
   - `SUPABASE_URL`
   - `SUPABASE_KEY`
   - `SUPABASE_DB_URL`
   - `AUTHORTODAY_LOGIN` (опционально)
   - `AUTHORTODAY_PASSWORD` (опционально)

5. **Убедитесь, что `packages.txt` пустой или содержит только названия пакетов (без комментариев)**

6. **Запустите приложение**

## Структура проекта

```
Kursach/
├── app.py                      # Главный файл приложения
├── pages/                      # Страницы Streamlit
│   ├── 1_🏠_Главная.py
│   ├── 2_📚_Книги.py
│   └── 3_🔍_Поиск.py
├── database/                   # Работа с базой данных
│   ├── repository_supabase.py  # Репозитории через Supabase SDK
│   ├── supabase_client.py      # Клиент Supabase
│   └── helpers.py              # Вспомогательные классы
├── services/                   # Бизнес-логика
│   ├── author_today_api.py    # Интеграция с AuthorToday API
│   ├── fb2_parser.py          # Парсинг FB2 файлов
│   └── search.py              # Текстовый поиск
├── utils/                      # Утилиты
│   └── config.py              # Конфигурация
├── scripts/                    # Вспомогательные скрипты
│   ├── add_books_from_files.py
│   ├── init_database.py
│   └── ...
├── sql/                        # SQL скрипты
│   └── create_tables.sql
├── books/                      # FB2 файлы книг
├── requirements.txt            # Python зависимости
├── packages.txt               # Системные пакеты (для Streamlit Cloud)
└── README.md                  # Документация
```

## Полезные скрипты

- `scripts/add_books_from_files.py` - добавление книг из папки `books/`
- `scripts/init_database.py` - инструкции по созданию таблиц
- `scripts/create_env_interactive.py` - интерактивное создание `.env` файла
- `scripts/check_dependencies.py` - проверка установленных зависимостей

## Примечания

- Для работы приложения необходимо настроить RLS политики в Supabase
- FB2 файлы должны быть размещены в папке `books/`
- AuthorToday API требует авторизации для получения отзывов
- Для production рекомендуется использовать Service Role Key (обходит RLS)

## Лицензия

Проект создан в учебных целях.
